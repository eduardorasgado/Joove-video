import React from 'react';
import ReactDOM from 'react-dom';

const container = document.getElementById("app");

const holaReact = <h1>Hola React</h1>;

//Renderizamos
ReactDOM.render(holaReact,container);
